# Home Project

This is an Angular application called 'home'.

## Installation

Run `npm install` to install the necessary dependencies.

## Development server

Run `npm start` to start the development server.

## Running unit tests

Run `npm test` to execute the unit tests.
